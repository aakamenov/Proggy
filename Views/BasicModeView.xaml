<Page x:Class="Proggy.Views.BasicModeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:controls="clr-namespace:Proggy.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="250" d:DesignWidth="350"
      Title="BasicModeView">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <material:Card>
            <controls:TimeSignatureControls DataContext="{Binding TimeSignatureControls}" />
        </material:Card>
        <material:Card>
            <controls:GlobalControls DataContext="{Binding GlobalControls}" />
        </material:Card>
    </StackPanel>
</Page>
