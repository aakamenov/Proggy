<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Proggy.Controls"
             xmlns:material="clr-namespace:Material.Styles;assembly=Material.Styles"
             mc:Ignorable="d" d:DesignWidth="350" d:DesignHeight="250"
             x:Class="Proggy.Views.BasicModeView">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
      <material:Card>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
          <ComboBox SelectedItem="{Binding SelectedBeats}" Items="{Binding Beats}"></ComboBox>
          <ComboBox SelectedItem="{Binding SelectedNoteLength}" Items="{Binding NoteLengths}"></ComboBox>
          <NumericUpDown Value="{Binding TempoNumericBoxValue}" ClipValueToMinMax="True" Minimum="33" Maximum="300"/>
        </StackPanel>
      </material:Card>
      <material:Card>
        <controls:GlobalControls DataContext="{Binding GlobalControls}"></controls:GlobalControls>
      </material:Card>  
    </StackPanel>
</UserControl>
