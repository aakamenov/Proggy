<UserControl x:Class="Proggy.Controls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">

    <StackPanel Orientation="Horizontal">
        <TextBox Height="30" Name="TextBox" TextAlignment="Center" />
        <Grid Height="40" Width="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>
            <RepeatButton Name="UpButton" Grid.Row="0" VerticalContentAlignment="Center" FontSize="10" HorizontalContentAlignment="Center" Click="OnUpButtonClick">˄</RepeatButton>
            <RepeatButton Name="DownButton" Grid.Row="1" VerticalContentAlignment="Center" FontSize="10" HorizontalContentAlignment="Center" VerticalAlignment="Bottom" Click="OnDownButtonClick">˅</RepeatButton>
        </Grid>
    </StackPanel>

</UserControl>
